安装mongodb(加path)。nodejs。
npm install
mongochef免费。

运行：
startDatabase.bat
startWeb.bat

安装git
git config --global user.name "KK"
git config --global user.email wyklion@qq.com
git clone https://github.com/wyklion/kkTetris
